import PubInputForm from "./PubInputForm"

// todo It would act as a single place to render all the component of the form
const PubForm = () => {
  return (
    <PubInputForm/>
  )
}

export default PubForm