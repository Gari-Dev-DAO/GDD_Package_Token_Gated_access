export const newFunction = () => {};

export const checkForPublisher = async (userAddrs: string): Promise<boolean> => {
  //make the api call and then
  //spit the true and false with promise
  return true;
};
